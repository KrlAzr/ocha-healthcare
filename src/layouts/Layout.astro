---
import { Menu, X, Search } from 'lucide-react';
const { title } = Astro.props;
const currentPath = Astro.url.pathname;
const WA_LINK = "https://wa.me/60166062534";
---

<!doctype html>
<html lang="en" class="scroll-smooth">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400;0,500;0,600;0,700;1,400&family=Plus+Jakarta+Sans:wght@300;400;500;600;700&display=swap" rel="stylesheet">
		
		<title>{title}</title>
	</head>
	
	<body class="bg-slate-50 font-sans text-slate-900 antialiased selection:bg-indigo-100 selection:text-indigo-900 flex flex-col min-h-screen">
		
		{/* === NAVBAR (Glassmorphism) === */}
		<nav class="fixed w-full z-50 bg-white/95 backdrop-blur-md border-b border-slate-200 transition-all duration-300">
			<div class="max-w-7xl mx-auto px-6 h-20 flex justify-between items-center relative">
				
				{/* 1. LOGO (Left) */}
				<a href="/" class="flex-shrink-0 opacity-100 hover:opacity-80 transition-opacity z-50">
					<img 
						src="/assets/logo.png" 
						alt="Ocha Healthcare" 
						class="h-10 w-auto object-contain"
					/>
				</a>

				{/* 2. ACTION BUTTONS (Right - Desktop Only) */}
				<div class="hidden md:flex items-center gap-3">
					
					{/* Secondary Button: Find Specialist */}
					<a 
						href="/doctors" 
						class="flex items-center gap-2 px-5 py-2.5 rounded-lg text-sm font-bold text-slate-700 bg-white border border-slate-200 hover:bg-slate-50 hover:border-slate-300 transition-all"
					>
						<Search className="w-4 h-4 text-slate-400" />
						Find Specialist
					</a>

					{/* Primary Button: Book Consultation */}
					<a 
						href={WA_LINK}
						target="_blank"
						class="bg-indigo-600 text-white px-5 py-2.5 rounded-lg text-sm font-bold hover:bg-indigo-700 transition shadow-lg shadow-indigo-600/20 active:scale-95 transform duration-150"
					>
						Book Consultation
					</a>
				</div>

				{/* 3. MOBILE MENU TOGGLE BUTTON */}
				<button id="menu-btn" class="md:hidden text-slate-600 p-2 z-50 focus:outline-none">
					<div class="icon-menu block">
						<Menu className="w-6 h-6" />
					</div>
					<div class="icon-close hidden">
						<X className="w-6 h-6" />
					</div>
				</button>
			</div>
		</nav>

		{/* === MOBILE MENU OVERLAY === */}
		<div 
			id="mobile-menu" 
			class="fixed inset-0 z-40 bg-white transform translate-x-full transition-transform duration-300 md:hidden flex flex-col pt-24 px-6 gap-6"
		>
			<a href="/" class="text-xl font-medium text-slate-900 border-b border-slate-100 pb-4">
				Concierge Home
			</a>
			<a href="/doctors" class="text-xl font-medium text-slate-900 border-b border-slate-100 pb-4 flex items-center justify-between">
				Find Specialist <Search className="w-5 h-5 text-slate-400" />
			</a>
			
			<a 
				href={WA_LINK} 
				target="_blank" 
				class="bg-indigo-600 text-white text-center py-4 rounded-xl font-bold text-lg mt-4 shadow-xl shadow-indigo-200 active:scale-95 transition-transform"
			>
				Book Consultation
			</a>
		</div>

		{/* === MAIN CONTENT === */}
		<div class="flex-grow">
			<slot />
		</div>

		{/* === FOOTER === */}
		<footer class="bg-white border-t border-slate-200 py-12 mt-auto">
			<div class="max-w-7xl mx-auto px-6 flex flex-col md:flex-row justify-between items-center gap-6">
				<div class="opacity-80 grayscale hover:grayscale-0 transition-all duration-500">
					<img src="/assets/logo.png" alt="Ocha Healthcare" class="h-8 w-auto object-contain" />
				</div>
				<div class="text-slate-400 text-sm text-center md:text-right font-medium">
					© 2025 Ocha Healthcare. All rights reserved.
				</div>
			</div>
		</footer>

		{/* === MOBILE MENU SCRIPT === */}
		<script>
			const menuBtn = document.getElementById('menu-btn');
			const mobileMenu = document.getElementById('mobile-menu');
			const iconMenu = document.querySelector('.icon-menu');
			const iconClose = document.querySelector('.icon-close');
			let isMenuOpen = false;

			if (menuBtn && mobileMenu && iconMenu && iconClose) {
				menuBtn.addEventListener('click', () => {
					isMenuOpen = !isMenuOpen;
					
					if (isMenuOpen) {
						mobileMenu.classList.remove('translate-x-full');
						mobileMenu.classList.add('translate-x-0');
						document.body.classList.add('overflow-hidden');
						
						iconMenu.classList.remove('block');
						iconMenu.classList.add('hidden');
						iconClose.classList.remove('hidden');
						iconClose.classList.add('block');
					} else {
						mobileMenu.classList.remove('translate-x-0');
						mobileMenu.classList.add('translate-x-full');
						document.body.classList.remove('overflow-hidden');
						
						iconMenu.classList.remove('hidden');
						iconMenu.classList.add('block');
						iconClose.classList.remove('block');
						iconClose.classList.add('hidden');
					}
				});
				
				const links = mobileMenu.querySelectorAll('a');
				links.forEach(link => {
					link.addEventListener('click', () => {
						isMenuOpen = false;
						mobileMenu.classList.add('translate-x-full');
						document.body.classList.remove('overflow-hidden');
						iconMenu.classList.add('block');
						iconMenu.classList.remove('hidden');
						iconClose.classList.add('hidden');
					});
				});
			}
		</script>
	</body>
</html>

<style is:global>
	:root { 
		--font-sans: 'Plus Jakarta Sans', sans-serif; 
		--font-serif: 'Lora', serif; 
	}
	body { font-family: var(--font-sans); }
	h1, h2, h3, h4, h5, h6 { font-family: var(--font-serif); }
</style>